{
  "name": "mcp-workspace-gateway",
  "version": "0.1.0",
  "description": "Local-first MCP workspace gateway",
  "type": "commonjs",
  "bin": {
    "mcp-mux": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli/index.js",
    "test": "npm run build && node --test dist/test/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "25.7.0",
    "typescript": "6.0.3"
  }
}
